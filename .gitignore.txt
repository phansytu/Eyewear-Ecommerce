# --- Loại bỏ các thư mục build và tạm ---
build/
dist/
nbbuild/
nbdist/
nbactions.xml
nb-configuration.xml

# --- Loại bỏ các file rác của NetBeans ---
private/
nbproject/private/

# --- Loại bỏ file cấu hình riêng của từng máy ---
# (Chỉ giữ lại file cấu hình khung của dự án)
!nbproject/project.xml
!nbproject/project.properties

# --- Loại bỏ thư viện jar nếu bạn muốn người kia tự add ---
# (Nếu muốn dùng chung thư viện thì comment dòng dưới bằng dấu #)
# lib/

# --- Các file hệ thống khác ---
.DS_Store
Thumbs.db